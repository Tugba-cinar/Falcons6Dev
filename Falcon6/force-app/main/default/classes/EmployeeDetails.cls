public  class EmployeeDetails {
    public void showDetails(string nm, string eml, string phn) {
// String name = nm;
// string email = eml;
// string phone = phn;
system.debug('Employee name is ' +nm);
system.debug('Employee email is ' +eml);
system.debug('employee phone number is '+phn);
    }
}
